# GLM-5.3 Flash · Ox Alpha lab

Lab only. Pack-ready: **no**. Not in `templates/README.md`.

Audit round 2026-08-27 (refs → deltas; node --check passed on all three module scripts; browser eyeball owed):

- `01-glass-cubes.html` — cubes grown to poster share (~⅓ frame width: A 2.30, B 1.96) and re-seated ON the serif rows; dispersion 1.35→1.6 + attDist 3.4 for the saturated split through letterforms; cover-crop margin +1.5 so serif lines bleed harder off both edges.
- `02-glass-ribbon.html` — tail fan tightened and made radial earlier (rollRamp 0.22 / ×3.2); hero centerline raised (y 0.55) to clear the headline; benefits vortex pushed right+deep ([3.0, 1.05, −2.0]) like the coil in the Ox still; pink bloom band widened (uMin −0.18).
- `03-segmint-voxel.html` — grid R 16→20, cell 0.29 (finer minted grain, same ~12.3-world disc); rim ring darkened before the groove (r01>0.80, lerp 0.6); pixel wordmark scaled up (fontPx 72).
- Skill (`still-to-webgl-theater/SKILL.md`) tightened to match the HTML: wide-window stance + repaint/dispose rule; per-zone attenuation stated as class rule; type-as-texture test gained its inverse (no-refraction ⇒ 2D backdrop layer under alpha GL pass); reveal covers must share the revealed palette function.

Remaining gaps: visual deltas verified statically only — Liz eyeballs all three; benefits-pose coil depth and case-1 cube scale need a live look.

Refs mapping unchanged: `640.png`/`…102903` → 1 · `640.jpg`/`…102946` → 2 · `640 (1).png`/`…103019` → 3 · `640 (2).png` = noise.
